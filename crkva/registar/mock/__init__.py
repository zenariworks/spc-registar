"""Мок подаци + sources / sanity constraints за seed_* команде.

generators  — Cyrillic name/date/phone/address генератори (Python random)
constraints — провере (свештеник мора бити мушки одрастао, ...)
sources     — апстракција над извором: MockSource, FixtureSource, DbfSource
"""
